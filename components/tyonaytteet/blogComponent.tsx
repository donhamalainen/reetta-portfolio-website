import React from "react";

export default function blogComponent({
  title,
  description,
  slug,
}: {
  title: String;
  description: String;
  slug: String;
}) {
  return <div>blogComponent</div>;
}
